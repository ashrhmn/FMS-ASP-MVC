@model IEnumerable<Flight_Management_System.Models.AdminEntities.UserModel>
@{
    ViewBag.Title = "Userlist";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<h4>Search</h4>
@using (Html.BeginForm("Userlist", "Admin", FormMethod.Post))
{
    @Html.TextBox("searching")
    <input type="submit" value="Search" />
}
<h2>Customers</h2>
<table class="table table-bordered">
    <tr>
        <th>Name</th>
        <th>User Name</th>
        <th>Location</th>
        <th>Address</th>
        <th>Country name</th>
        <th>Actions</th>
    </tr>
    @foreach (var u in Model)
    {
        if (u.Role == 2)
        {

            <tr>
                <td>@u.Name</td>
                <td>@u.Username</td>
                <td>@u.Address</td>
                <td>@u.CityName, @u.CountryName</td>
                <td>@u.CountryName</td>
                <td>
                    <a href="/Admin/UserDetails/@u.Id">View Details</a>
                    <a href="/Admin/EditUserProfile/@u.Id">Edit</a>
                    <a href="/Admin/DeleteUser/@u.Id">Delete</a>
                </td>
            </tr>

        }
    }
</table>
<br />
<br />
<h2>Flight managers</h2>
<table class="table table-bordered">
    <tr>
        <th>Name</th>
        <th>User Name</th>
        <th>Location</th>
        <th>Address</th>
        <th>Country name</th>
        <th>Actions</th>
    </tr>
    @foreach (var u in Model)
    {
        if (u.Role == 2)
        {

            <tr>
                <td>@u.Name</td>
                <td>@u.Username</td>
                <td>@u.Address</td>
                <td>@u.CityName, @u.CountryName</td>
                <td>@u.CountryName</td>
                <td>
                    <a href="/Admin/UserDetails/@u.Id">Edit</a>
                    <a href="/Admin/EditUserProfile/@u.Id">Edit</a>
                    <a href="/Admin/DeleteUser/@u.Id">Delete</a>
                </td>
            </tr>
        }

    }
</table>



